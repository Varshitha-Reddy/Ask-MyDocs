fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12
python-dotenv==1.0.1

# LangChain — orchestrates document loading, chunking, and LLM calls
langchain==0.3.7
langchain-openai==0.2.5
langchain-community==0.3.5
langchain-text-splitters==0.3.2

# Document loaders
pypdf==5.0.1
unstructured==0.16.0

# Vector database (semantic search)
# Note: the package is now just "pinecone", not "pinecone-client"
pinecone==5.0.1

# Keyword search
elasticsearch==8.15.1

# Cache
redis==5.1.1

# Utilities
tenacity==9.0.0
loguru==0.7.2
aiofiles==24.1.0

# Observability — Prometheus metrics endpoint
prometheus-client==0.21.0

# Phase 4: Cohere re-ranking (4.3)
cohere==5.11.0

# Phase 4: RAGAS evaluation (4.5)
ragas==0.1.21
datasets==2.21.0

# Celery — production task queue (replaces FastAPI BackgroundTasks)
celery==5.4.0
# flower — web UI to monitor Celery tasks (optional but useful)
flower==2.0.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
